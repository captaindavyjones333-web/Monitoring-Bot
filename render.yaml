services:
  - type: web
    name: monitoring-bot
    env: docker
    region: frankfurt
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: CHAT_ID
        sync: false
      - key: NODE_ENV
        value: production